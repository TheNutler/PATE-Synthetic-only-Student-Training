This directory should contain trained model files (.pth or .pkl)
for teacher 1.

Example: Place your model file here as 'model.pth'
The model should be saved as a state_dict using:
  torch.save(model.state_dict(), 'model.pth')
